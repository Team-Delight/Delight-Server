config:
  target: "https://api.delight99.co.kr"
  # target: "http://3.38.100.139"
  phases:
    - duration: 300
      arrivalRate: 400
      name: Warm up
scenarios:
  # We define one scenario:
  - name: "Get Random Foods"
    flow:
      - get:
          url: "/api/foods"